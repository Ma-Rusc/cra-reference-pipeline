[project]
name = "api-python"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.3",
    "uvicorn[standard]>=0.32",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "httpx>=0.27",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
